import styled from 'styled-components';
import tokens from 'force-tokens';

export const ButtonStyled = styled.button`
  color: ${tokens.ColorGray000};
  background-color: ${tokens.ColorBlue};
  border: none;
  padding: 12px 34px;
  border-radius: 4px;
`;
